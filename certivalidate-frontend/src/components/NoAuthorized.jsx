import React from 'react';

export const NoAuthorized = () => (
  <div className="not-authorized-screen">
    <div className="not-authorized-card glass-panel">
      <h2>No autorizado</h2>
      <p>No tienes permiso para acceder a este contenido. Si crees que deberías tener acceso, contacta a un administrador.</p>
    </div>
  </div>
);
